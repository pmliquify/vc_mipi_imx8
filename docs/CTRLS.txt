

struct v4l2_ctrl_handler {
	u16 nr_of_buckets;               <-- 1
        struct v4l2_ctrl_ref *cached;    <-- 2 Optimization
	struct v4l2_ctrl_ref **buckets;  <-- 3 Wird als Liste oder Array behandel?

        struct list_head ctrl_refs;
        struct list_head ctrls;

	struct mutex _lock;
	struct mutex *lock;
	
	v4l2_ctrl_notify_fnc notify;
	void *notify_priv;
	int error;
	bool request_is_queued;
	struct list_head requests;
	struct list_head requests_queued;
	struct media_request_object req_obj;
};



v4l2_ctrl_new_custom                    // Erzeugt ein neues Ctrl
        v4l2_ctrl_new                   
                handler_new_ref         // Fügt eine neue Referenz auf das Ctrl dem Handler hinzu.

v4l2_queryctrl
        v4l2_query_ext_ctrl
               find_ref 


